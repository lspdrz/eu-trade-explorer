/**
 * Brussels — the EU institutional seat. Cosmetic: the single point every
 * import arc converges on. Not a data value. `[longitude, latitude]`.
 */
export const EU_ANCHOR: [number, number] = [4.35, 50.85];
