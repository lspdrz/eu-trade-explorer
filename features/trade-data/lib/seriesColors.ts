/**
 * The categorical colour ramp, in slot order. Validated (see app/globals.css)
 * — three is the max that clears all-pairs CVD separation in both themes.
 * Shared by every chart so a colour means the same thing across the page.
 */
export const SERIES_COLORS = [
  "var(--color-series-1)",
  "var(--color-series-2)",
  "var(--color-series-3)",
];
